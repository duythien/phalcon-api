# behat.yml

default:
    suites:
        domain_features:
            paths:    [ %paths.base%/features/domain ]
            contexts: [ DomainContext ]
        web_features:
            paths:    [ %paths.base%/features/web ]
            contexts: [ WebContext ]
            filters:
                tags: @web
